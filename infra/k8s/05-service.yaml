apiVersion: v1
kind: Service
metadata:
  name: tefca-gateway
  namespace: tefca
spec:
  type: ClusterIP
  selector:
    app: tefca-gateway
  ports:
    - name: http
      port: 8080
      targetPort: 8080
