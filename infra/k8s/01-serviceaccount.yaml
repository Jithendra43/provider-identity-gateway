# IRSA role ARN is substituted at deploy time (see .github/workflows/deploy.yml).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: tefca-gateway
  namespace: tefca
  annotations:
    eks.amazonaws.com/role-arn: "${POD_ROLE_ARN}"
